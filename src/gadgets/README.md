# Gadgets

Reserved for M4. No real gadget ships in the foundation. Implement `Gadget` from `shared/contracts.ts`; register at the composition root. Import only Shared and the module's own files. Request services through the injected context. Release all subscriptions on dispose.
